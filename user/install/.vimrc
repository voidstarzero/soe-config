" Maintain compatibility with Debian vim configuration
runtime! debian.vim

" Tabs are 4 spaces, always
set tabstop=4
set shiftwidth=4
set softtabstop=4
set expandtab

" Syntax highlighting is nice
filetype indent plugin on
syntax on
